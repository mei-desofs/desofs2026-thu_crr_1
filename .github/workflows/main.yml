name: main

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write
 
jobs:
  build-test:
    uses: ./.github/workflows/build-test.yml

  release-please:
    uses: ./.github/workflows/release.yml 