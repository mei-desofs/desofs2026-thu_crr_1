name: feature

on:
  push:
    branches-ignore:
      - dev
      - main
      - docs/*

jobs:
  build-test:
    uses: ./.github/workflows/build-test.yml

  sast:
    needs: build-test
    uses: ./.github/workflows/security-sast.yml
    secrets: inherit