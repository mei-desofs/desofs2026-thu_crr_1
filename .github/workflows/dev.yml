name: dev

on:
  push:
    branches:
      - dev

  workflow_dispatch:

jobs:
  build-test:
    uses: ./.github/workflows/build-test.yml

  sast:
    needs: build-test
    uses: ./.github/workflows/security-sast.yml
    secrets: inherit